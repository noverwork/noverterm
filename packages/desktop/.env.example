# Backend API
# Prefer sharing the same host/port settings as packages/backend/.env during local dev.
BACKEND_HOST=127.0.0.1
BACKEND_PORT=3000

# Optional explicit override for the desktop app.
# NOVERTERM_BACKEND_URL=http://127.0.0.1:3000
