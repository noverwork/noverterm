[package]
name = "shared"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
specta = { version = "=2.0.0-rc.20", features = ["derive"] }
tracing = "0.1"
